import React from "react";
import Selector from "./Selector";

const MyBookings = () => {
  return (
    <div>
      <h5 className="primary-font-color" style={{ marginBottom: "1em" }}>
        MyBookings
      </h5>

      <Selector />
    </div>
  );
};

export default MyBookings;
